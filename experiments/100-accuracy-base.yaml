gpus: [1, 2]
memory_fraction: 0.7
server_seed: 1234
max_running_requests: 512
num_concurrent: 512
timeout: 28800

tasks: tasks.yaml
results_root: results

models:
  Ouro-1.4B:
    configs:
      baseline:
        sampling:
          greedy:
            tasks: [gsm8k-cot, math500, bbh, humaneval, humaneval-plus, mbpp, mbpp-plus]

  Ouro-2.6B:
    configs:
      baseline:
        sampling:
          greedy:
            tasks: [gsm8k-cot, math500, bbh, humaneval, humaneval-plus, mbpp, mbpp-plus]

  Recurrent-Llama-3.2-train-recurrence-32:
    configs:
      baseline:
        sampling:
          greedy:
            tasks: [gsm8k-cot, math500, bbh, humaneval, humaneval-plus, mbpp, mbpp-plus]

  Recurrent-OLMo-2-0425-train-recurrence-32:
    configs:
      baseline:
        sampling:
          greedy:
            tasks: [gsm8k-cot, math500, bbh, humaneval, humaneval-plus, mbpp, mbpp-plus]

  Recurrent-TinyLlama-3T-train-recurrence-32:
    configs:
      baseline:
        sampling:
          greedy:
            tasks: [gsm8k-cot, humaneval, humaneval-plus, mbpp-plus]
